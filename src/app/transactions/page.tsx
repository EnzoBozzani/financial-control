import styles from './page.module.scss';

const Transactions = () => {

    return (
        <section className={styles.wrapper}>
            <header>
                <p>Transactions</p>
            </header>
        </section>
    )
}

export default Transactions;